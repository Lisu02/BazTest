package com.example.firebasedatabase;

import androidx.annotation.NonNull;

public enum Kategoria {
    P,K,S,D,PK,PS,PD,KS,KD,SD,PKS,PKD,PSD,KSD,PKSD;

    @NonNull
    @Override
    public String toString() {
        return super.toString();
    }
}
